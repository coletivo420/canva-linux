# Canva Linux CLI

Run `./canva-linux.sh` without arguments to open c420ui. Pass one direct action flag to execute that action through the c420ui CLI bridge and Action Engine.

Use `./canva-linux.sh --help` for stable launcher help. Use `npm run c420ui:cli -- --help` to inspect the dynamic action list generated from the Canva Linux action registry.
