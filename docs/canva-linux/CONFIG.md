# Canva Linux Configuration

Canva Linux project configuration is centralized in `config/canva-linux/`:

- `actions.json` declares action metadata and commands;
- `artifacts.json` declares artifact workflows and release output patterns;
- `dependencies.json` declares host and npm dependency requirements;
- `development.json` declares development tasks;
- `project-ui.json` declares project metadata displayed by c420ui.
